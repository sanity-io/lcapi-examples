# Set on any deployment that the studio's `cache-invalidate` Sanity Function targets.
# Production on Vercel (VERCEL_ENV=production) is treated as targeted without it.
SANITY_SYNC_TAG_INVALIDATE_FUNCTION=true
# Optional bearer token /api/revalidate-tags requires; give the function the same value.
SANITY_REVALIDATE_SECRET=
