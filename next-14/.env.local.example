# Set when the studio `cache-invalidate` function targets this deployment (see README.md).
# Production on Vercel (VERCEL_ENV=production) behaves as if this is set.
SANITY_SYNC_TAG_INVALIDATE_FUNCTION=
